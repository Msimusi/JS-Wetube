extends base



block content 
    video(src="/"+video.fileUrl, controls, width="800", height="450")
    div
        p=video.description
        small=video.createdAt
    a(href=`${video.id}/edit`) Edit Video &rarr;
    a(href=`${video.id}/delete`) Delete Video &rarr;